#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double w, z;

    cout << "Enter z: ";
    cin >> z;

    if ((-2 <= z) && (z <= 0.5))
    {
        w = z - sin(z);
    } else if ((0.5 <= z) && (z <= 3)) {
        for ()
        {
            
        }
    }
    

    return 0;
}